import sys
from PyQt6.QtCore import QUrl, QTimer
from PyQt6.QtWidgets import QApplication
from PyQt6.QtWebEngineCore import QWebEngineProfile
from PyQt6.QtWebEngineWidgets import QWebEngineView

def run_global_king():
    app = QApplication(sys.argv)
    browser = QWebEngineView()
    
    # Aapka Facebook Video Link
    video_url = "https://www.facebook.com/share/v/16zN1Fo3qf/"
    
    # USA Identity Setup
    profile = QWebEngineProfile.defaultProfile()
    profile.setHttpUserAgent("Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36")
    
    print(f"🚀 Signal Active: Sending to Rahim Yar Khan via USA Server...")
    browser.load(QUrl(video_url))
    
    # Screenshot Function (Result Dekhne Ke Liye)
    def take_screenshot():
        browser.grab().save("result_signal.png")
        print("📸 Screenshot Saved: Check your repository for 'result_signal.png'")

    # 30 second baad screenshot lega
    QTimer.singleShot(30000, take_screenshot)
    
    browser.show()
    sys.exit(app.exec())

if __name__ == "__main__":
    run_global_king()
